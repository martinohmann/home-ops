---
apiVersion: v1
kind: Secret
metadata:
  name: cloudnative-pg
stringData:
  username: "${SECRET_POSTGRES_SUPER_USER}"
  password: "${SECRET_POSTGRES_SUPER_PASSWORD}"
  aws-access-key-id: "${SECRET_CLOUDNATIVE_PG_MINIO_ACCESS_KEY_ID}"
  aws-secret-access-key: "${SECRET_CLOUDNATIVE_PG_MINIO_SECRET_ACCESS_KEY}"
