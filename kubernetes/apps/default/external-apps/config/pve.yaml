---
apiVersion: v1
kind: Service
metadata:
  name: pve
  namespace: default
spec:
  externalName: pve.18b.lan.
  type: ExternalName

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/target: ingress.${SECRET_DOMAIN}
    nginx.ingress.kubernetes.io/backend-protocol: HTTPS
    hajimari.io/appName: Proxmox VE
    hajimari.io/group: virtualization
    hajimari.io/icon: simple-icons:proxmox
  name: pve
  namespace: default
spec:
  ingressClassName: internal
  rules:
    - host: pve.${SECRET_DOMAIN}
      http:
        paths:
          - backend:
              service:
                name: pve
                port:
                  number: 8006
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - pve.${SECRET_DOMAIN}
