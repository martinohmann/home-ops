---
apiVersion: v1
kind: Secret
metadata:
  name: home-assistant-volsync
stringData:
  RESTIC_REPOSITORY: s3:http://minio.default.svc.cluster.local:9000/volsync/home-assistant
  RESTIC_PASSWORD: "${SECRET_RESTIC_PASSWORD}"
  AWS_ACCESS_KEY_ID: "${SECRET_VOLSYNC_MINIO_ACCESS_KEY_ID}"
  AWS_SECRET_ACCESS_KEY: "${SECRET_VOLSYNC_MINIO_SECRET_ACCESS_KEY}"
